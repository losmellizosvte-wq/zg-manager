rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permitir acceso total (lectura y escritura) a cualquier usuario autenticado
      allow read, write: if request.auth != null;
    }
  }
}
